@model Pbl.Models.ViewModel.DisciplinaViewModel
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutDiretor.cshtml";
}

<script type="text/javascript">
    $mensagem = "@ViewBag.Message";
    if ($mensagem) {
        alert($mensagem);
    }
</script>
<div id="content-header">
    <div id="breadcrumb"> <a href="@Url.Action("Index","Home")" title="Vá para Home" class="tip-bottom"><img src="~/Content/img/home.png" class="home" />Home</a><a href="@Url.Action("Index","ControleDisciplinas")" title="Vá para Controle Disciplinas" class="tip-bottom"><img src="~/Content/img/client.png" class="home" />Disciplinas</a></div>
</div>
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span6">
            <div class="widget-box">
                <form action="" method="post" class="form-styles">
                    <h1>Cadastro de Problemas</h1>
                   @using (Html.BeginForm("Create", "ControleDisciplinas"))
                   {
                        @Html.ValidationSummary(true, "Algo não está certo");
                        <ul>
                            <li>
                                <textarea name="descDisciplina" placeholder="Descrição"></textarea>
                                <span>Informe a descrição</span>
                            </li>
                            <li>
                                <select id="disciplina_idTipoDisciplina" name="c.disciplina.idTipoDisciplina">
                                    <option value="@Model.listaTipoDisciplina"></option>
                                </select>
                            </li>
                            @Html.AntiForgeryToken()
                            <li>
                                <input type="submit" value="Salvar" />
                            </li>
                        </ul>
                    }
                </form>
            </div>
        </div>
    </div>
</div>

@*

    <h2>Cadastro de Disciplinas</h2>

    @using (Html.BeginForm("Create", "ControleDisciplinas"))
    {
        @Html.ValidationSummary(true, "Algo não está certo");
        <div class="form-group">
            <label for="descDisciplina">Disciplina</label>
            @Html.TextBoxFor(c => c.disciplina.descDisciplina, new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.disciplina.descDisciplina)
        </div>
        <div class="form-group">
            <label>Tipo Disciplina</label><br />
            @Html.DropDownListFor(c => c.disciplina.idTipoDisciplina, new SelectList(Model.listaTipoDisciplina, "idTipoDisciplina", "descTipoDisciplina"), "", new { @class = "form-control" })
            @Html.ValidationMessageFor(c => c.disciplina.idTipoDisciplina)
        </div>
        @Html.AntiForgeryToken()
        <button type="submit" class="btn btn-primary">Salvar</button>
    }
*@
