@model Pbl.Models.Problema
@{
    ViewBag.Title = "Create";
    Layout = "~/Views/Shared/_LayoutDiretor.cshtml";
}
<script type="text/javascript">
    $mensagem = "@ViewBag.Message";
    if ($mensagem) {
        alert($mensagem);
    }
</script>
<div id="content-header">
    <div id="breadcrumb"> <a href="@Url.Action("Index","Home")" title="Vá para Home" class="tip-bottom"><img src="~/Content/img/home.png" class="home" />Home</a><a href="@Url.Action("Index","ControleProblemas")" title="Vá para Controle Problemas" class="tip-bottom"><img src="~/Content/img/client.png" class="home" />Problemas</a></div>
</div>
<div class="container-fluid">
    <div class="row-fluid">
        <div class="span6">
            <div class="widget-box">
                <form action="" method="post" class="form-styles">
                    <h1>Cadastro de Problemas</h1>
@using (Html.BeginForm("Create", "ControleProblemas"))
{
    @Html.ValidationSummary(true, "Algo deu errado")
                    <ul>
                        <li>
                            <input name="tituloProblema" type="text" placeholder="Titulo" />
                            <span>Informe o titulo</span>
                        </li>
                        <li>
                            <textarea name="descProblema" placeholder="Descrição"></textarea>
                            <!--<input name="descProblema" type="text" placeholder="Descrição" />-->
                            <span>Informe a descrição</span>
                        </li>
                        @Html.AntiForgeryToken()
                        <li>
                            <input type="submit" value="Salvar" />
                        </li>
                    </ul>
}
                </form>
            </div>
        </div>
    </div>
</div>

@*
<h2>Cadastro de Problemas</h2>

@using (Html.BeginForm("Create", "ControleProblemas"))
{
    @Html.ValidationSummary(true, "Algo deu errado")
    <div class="form-group">
        <label for="tituloProblema">Título</label>
        @Html.TextBoxFor(c => c.tituloProblema, new { @class = "form-control" })
        @Html.ValidationMessageFor(c => c.tituloProblema)
    </div>
                    <div class="form-group">
                        <label for="descProblema">Descrição</label>
                        @Html.TextAreaFor(c => c.descProblema, new { @class = "form-control" })
                        @Html.ValidationMessageFor(c => c.descProblema)
                    </div>
                    @Html.AntiForgeryToken()
                    <button type="submit" class="btn btn-primary">Salvar</button>
}*@

