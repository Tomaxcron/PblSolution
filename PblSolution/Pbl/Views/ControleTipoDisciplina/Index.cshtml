@model IEnumerable<Pbl.Models.TipoDisciplina>
@{
    ViewBag.Title = "Tipo disciplina";
    Layout = "~/Views/Shared/_LayoutDiretor.cshtml";
}
<script type="text/javascript">
    $mensagem = "@ViewBag.Message";
    if ($mensagem) {
        alert($mensagem);
    }
</script>

<div id="content-header">
    <div id="breadcrumb"> <a href="@Url.Action("Index","Home")" title="Vá para Home" class="tip-bottom"><img src="~/Content/img/home.png" class="home" />Home</a><a href="@Url.Action("Index","ControleTipoDisciplina")" title="Vá para Controle Tipo Disciplinas" class="tip-bottom"><img src="~/Content/img/client.png" class="home" />Tipo disciplina</a></div>
</div>
<h2>Index @Html.ActionLink("Adicionar", "Create", null, new { @class = "classname" })</h2>

<div class="container-fluid">
    <div class="row-fluid">
        <div class="span12">
            <div class="widget-box">

                <div class="widget-content nopadding">
                    <div class="table-responsive">
                        <table class="table table-striped table-hover" id="myTable">
                            <thead>
                                <tr>
                                    <th>Descrição</th>
                                    <th></th>
                                </tr>
                            </thead>
                            <tbody>
                                @foreach (var item in Model)
                                {
                                    <tr class="gradeX">
                                        <td>@Html.DisplayFor(c => item.descTipoDisciplina)</td>
                                        <td>
                                            <img class="actimg" src="~/Content/img/edit.png" />  @Html.ActionLink("Alterar", "Update", new { id = item.idTipoDisciplina }, new { @class = "alter" })
                                            <img class="actimg" src="~/Content/img/delet.png" /> @Html.ActionLink("Deletar", "Delete", new { id = item.idTipoDisciplina }, new { @class = "alter" })
                                        </td>
                                    </tr>
                                }
                            </tbody>
                        </table>
                    </div>
                </div>
            </div>
        </div>
    </div>

</div>

@*
    <table class="table">
        <tr>
            <th>Descrição</th>
            <th></th>
        </tr>
        @if (Model != null)
        {
            foreach (var item in Model)
            {
                <tr>
                    <td>@Html.DisplayFor(c => item.descTipoDisciplina)</td>
                    <td>
                        @Html.ActionLink("Alterar", "Update", new { id = item.idTipoDisciplina, @class = "btn btn-primary" })
                        @Html.ActionLink("Deletar", "Delete", new { id = item.idTipoDisciplina, @class = "btn btn-primary" })
                    </td>
                </tr>
            }
        }
    </table>
*@